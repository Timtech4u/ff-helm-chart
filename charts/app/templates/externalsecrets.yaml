{{- with .Values.secrets -}}
apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  labels:
    {{- include "microservice-ff.labels" $ | nindent 4 }}
  name: {{ .name }}
spec:
  backendType: {{ .backend }}
  projectId: {{ .projectId }}
  data:
    {{- range .data }}
    - key: {{ .key }}
      name: {{ .name }}
    {{- end }}
  template:
    metadata:
      labels:
        {{- include "microservice-ff.labels" $ | nindent 8 }}
{{- end }}