apiVersion: v1
kind: Service
metadata:
  name: {{ include "microservice-ff.fullname" . }}
  labels:
    {{- include "microservice-ff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range $i, $val := .Values.ports }}
    - port: {{ $val.number }}
      targetPort: {{ $val.name }}
      protocol: TCP
      name: {{ $val.name }}
    {{- end }}
  selector:
    {{- include "microservice-ff.selectorLabels" . | nindent 4 }}
